# Contributing

PRs are welcome.

## Dev setup

```bash
pnpm install
pnpm dev
```

## Guidelines

- Keep UI changes minimal unless a change is specifically requested.
- Never commit secrets (private keys, API keys, seed phrases). Use `.env.local`.
